<Window x:Class="WpfFatigueMK2.PlayerDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Player Details" Height="200" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Player Name:" FontWeight="Bold"/>
        <TextBlock x:Name="PlayerNameTextBlock" Grid.Row="0" Margin="120,0,0,0"/>

        <TextBlock Text="Player Number:" Grid.Row="1" FontWeight="Bold"/>
        <TextBlock x:Name="PlayerSlotTextBlock" Grid.Row="1" Margin="120,0,0,0"/>

        <TextBlock Text="Average Steps/Game:" Grid.Row="2" FontWeight="Bold"/>
        <TextBlock x:Name="PlayerAvgStepsTextBlock" Grid.Row="2" Margin="120,0,0,0"/>
    </Grid>
</Window>
